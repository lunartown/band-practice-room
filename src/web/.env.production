# Vercel 외 production 모드 빌드의 기본 환경변수.
# Vercel 빌드는 `npm run build:vercel` 이 브랜치별 API URL을 process.env로 주입한다.
#   - main/stg: hapjusil-api(prod)
#   - dev: hapjusil-api-dev(dev)
#
# 비밀값 없음(공개 API URL뿐)이라 커밋한다.
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=https://hapjusil-api.onrender.com/api/v1
